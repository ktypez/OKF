---
type: document
id: DOC-003
project: data.mcky.space
last_updated: '2026-07-11'
status: active
freshness: '2026-07-11'
verified: '2026-07-11'
expires: null
superseded_by: null
anchors: []
links:
  - type: relates-to
    target: DOC-001
---

# DOC-003: Filter button show number

# Filter button show number

**File:** `src/components/FilterDropdown.tsx`

The filter trigger button currently shows only the current filter label (e.g., "ทั้งหมด"). It should also display the count for the active filter (e.g., "ทั้งหมด 42") directly on the button so the user sees the total without opening the dropdown.

- [ ] Show `counts[activeFilter]` next to `currentLabel` in the trigger button
- [ ] Keep the existing layout — icon + label + count, same text style
